# hephaes-core

Core Python package for ROS bag reading, profiling, mapping, Parquet conversion, and Parquet streaming.

## Install Notes
- Install from source:
  - `pip install .`
- Install from source with parquet support:
  - `pip install ".[parquet]"`
- Base install includes reader/profiler/mapping logic.